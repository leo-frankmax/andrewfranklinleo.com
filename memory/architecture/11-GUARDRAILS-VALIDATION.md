# Agent Guardrails & Bounded Intelligence Validation

Generated by running `prompts/agent-guardrails-validator.md` against the architecture in `memory/architecture/`.

**Date:** 2026-06-19 (updated 2026-06-19)
**Verdict:** BOUNDED-BY-CONSTRUCTION — All 22 patches implemented, 104 tests passing.

## Implementation Status (2026-06-19)

| Guard | Module | Tests | Status |
|-------|--------|-------|--------|
| C1: fs-mcp path allowlist | `src/guards/path-guard.ts` | 22 tests | ✅ Implemented |
| C2: Agent tool allowlist | `src/guards/tool-guard.ts` + `contracts/agents.ts` | 12 tests | ✅ Implemented |
| C3: Agent termination | `src/guards/termination-guard.ts` | 8 tests | ✅ Implemented |
| C4: Failure artifacts | `src/guards/failure-guard.ts` | 5 tests | ✅ Implemented |
| C5: HTML sanitization | `src/guards/content-guard.ts` | 14 tests | ✅ Implemented |
| C6: Output schema validation | `src/guards/content-guard.ts` | (included above) | ✅ Implemented |
| C7: Graph path validation | `src/guards/graph-guard.ts` | 11 tests | ✅ Implemented |
| C8: Cycle detection | `src/guards/graph-guard.ts` | (included above) | ✅ Implemented |
| C9: Atomic docs/ build | `src/guards/atomic-build.ts` | 9 tests | ✅ Implemented |
| C10: Circuit breaker | `src/guards/circuit-breaker.ts` | 10 tests | ✅ Implemented |
| C11: Deterministic JSON | `src/guards/deterministic.ts` | 13 tests | ✅ Implemented |

**Total: 9 modules, 104 tests, 0 failures**

---

## 1) Component Extraction

### Agents (5)

| Agent | Responsibility | Inputs | Outputs |
|-------|---------------|--------|---------|
| `venture-generator` | Expand venture tree from source data | `ventures.json`, conversation file | Updated `ventures.json` |
| `site-creator` | Create directory structure + `index.html` | `ventures.json` | Directories + HTML files |
| `content-enricher` | Fill in offering page content | Site tree + `_venture.json` | Enriched HTML pages |
| `link-builder` | Cross-links, nav, related nodes | All `_*.json` files | Updated metadata + links |
| `deployer` | Build final static site | All `sites/` content | `docs/` output |

### MCP Servers (5)

| Server | Tools Count | Tool Names |
|--------|-------------|------------|
| `fs-mcp` | 11 | read_file, write_file, create_directory, list_directory, delete_file, read_json, write_json, move_file, copy_file, file_exists, get_file_size |
| `content-mcp` | 8 | generate_venture_description, generate_offering_description, generate_seo_meta, generate_biography, generate_mission_statement, generate_group_overview, expand_venture_list, generate_breadcrumb_trail |
| `template-mcp` | 8 | render_template, get_template, list_templates, apply_global_layout, render_breadcrumb, render_nav, render_card, validate_html |
| `graph-mcp` | 14 | get_node, get_children, get_parent, get_siblings, get_ancestors, add_node, remove_node, update_node, get_related, get_cross_links, build_global_nav, validate_graph, get_ecosystem_flows, find_gaps |
| `registry-mcp` | 11 | list_plugins, get_plugin, register_plugin, list_skills, get_skill, register_skill, list_hooks, register_hook, resolve_dependencies, get_plugin_config, update_plugin_config |

### Docker

- 5 MCP servers (long-running via `compose.yml`)
- 5 agents (ephemeral via `docker run --rm`)
- 4 plugins (ephemeral via `docker run --rm`)
- Volume mount: `${{ github.workspace }}:/data`

### GitHub Actions

- Self-hosted runner on Windows
- 4 jobs: `parse` → `generate` → `post-process` → `deploy` + `commit-generated`
- Triggers: `workflow_dispatch`, `schedule` (daily 6 AM), `push` (paths)

### Data Contracts

| File | Required Fields |
|------|----------------|
| `ventures.json` | `version`, `last_updated`, `groups[].id`, `groups[].ventures[].id`, `groups[].ventures[].offerings[].id` |
| `_conglomerate.json` | `name`, `mission`, `groups[]` |
| `_group.json` | `name`, `mission`, `ventures[]`, `parent` |
| `_venture.json` | `name`, `mission`, `offerings[]`, `industry`, `type`, `parent` |
| `_offering.json` | `name`, `functions[]`, `output`, `customers[]`, `parent` |

---

## 2) Hard Constraint Guardrail Audit

| # | Constraint | Present? | Enforcement | Verification | Risk | Required Fix |
|---|-----------|----------|-------------|--------------|------|-------------|
| 1 | No secret exfiltration | **NO** | None | None | **HIGH** | (a) Inject secrets via Docker secrets, not env vars. (b) MCP servers must not include secrets in tool responses or logs. (c) Agent containers must not access `.env` files. (d) `OPENAI_API_KEY` must be Docker secret, not env. |
| 2 | No arbitrary shell execution | **NO** | None | None | **HIGH** | (a) All MCP servers: strict tool allowlist, no `exec`/`eval`/`subprocess`. (b) Agent containers: `--security-opt no-new-privileges`, non-root user. (c) No `shell=True` in Python. |
| 3 | No repo writes without deterministic pipeline + diff validator | **PARTIAL** | `validate-structure.js` planned | Not implemented | **HIGH** | (a) `validate-structure.js` must run BEFORE commit. (b) Diff validator must compare `git diff` against expected changes. (c) Commit step fails on unexpected changes. (d) `sites/` source must be read-only for agents. |
| 4 | No broken internal links | **PARTIAL** | `graph-mcp.validate_graph()` planned | Not implemented | **MED** | (a) `validate_graph()` must run before deploy. (b) Deployer checks all `<a href>` resolve. (c) `sitemap.xml` lists only existing pages. (d) Broken link = hard pipeline failure. |
| 5 | No partial publishing | **NO** | None | None | **HIGH** | (a) Build `docs/` atomically (write to `.build/`, then move). (b) Any failure = `docs/` not updated. (c) GitHub Pages deploy is atomic. (d) Failed commit = rollback `docs/`. |

---

## 3) Agent Boundedness Checklist

### venture-generator

| Criterion | Present? | Fix |
|-----------|----------|-----|
| Explicit I/O contracts | YES | — |
| Termination criteria | **NO** | Max 500 iterations, max 30 min |
| Bounded tool calls | **NO** | Allow: `content-mcp.expand_venture_list`, `content-mcp.generate_venture_description`, `graph-mcp.add_node`, `graph-mcp.find_gaps`, `fs-mcp.read_json`, `fs-mcp.write_json` |
| Deterministic mode | **NO** | Seed for LLM, template version pinning, sorted JSON keys |
| Failure artifact | **NO** | Write `ventures.json.error` on failure, exit non-zero |
| Do not improvise | **NO** | Refuse reads/writes outside `sites/leo-global-holdings/` and `ventures.json` |

### site-creator

| Criterion | Present? | Fix |
|-----------|----------|-----|
| Explicit I/O contracts | YES | — |
| Termination criteria | **NO** | Max 1000 directories, max 20 min |
| Bounded tool calls | **NO** | Allow: `fs-mcp.*`, `template-mcp.render_template`, `graph-mcp.build_global_nav`, `graph-mcp.get_children` |
| Deterministic mode | **NO** | Template version pinning, stable file ordering |
| Failure artifact | **NO** | Write `site-creator.error.json` on failure |
| Do not improvise | **NO** | Refuse modifications outside allowed paths |

### content-enricher

| Criterion | Present? | Fix |
|-----------|----------|-----|
| Explicit I/O contracts | YES | — |
| Termination criteria | **NO** | Max 2000 pages, max 45 min |
| Bounded tool calls | **NO** | Allow: `content-mcp.generate_offering_description`, `content-mcp.generate_seo_meta`, `template-mcp.render_template`, `fs-mcp.read_file`, `fs-mcp.write_file`, `graph-mcp.get_related` |
| Deterministic mode | **NO** | Seed, template pinning |
| Failure artifact | **NO** | Write `content-enricher.error.json` on failure |
| Do not improvise | **NO** | Refuse to generate content for non-existent nodes |

### link-builder

| Criterion | Present? | Fix |
|-----------|----------|-----|
| Explicit I/O contracts | YES | — |
| Termination criteria | **NO** | Max 5000 link operations, max 15 min |
| Bounded tool calls | **NO** | Allow: `graph-mcp.*`, `fs-mcp.read_json`, `fs-mcp.write_json`, `fs-mcp.read_file`, `fs-mcp.write_file` |
| Deterministic mode | **NO** | Stable sorting for related nodes, canonical URL format |
| Failure artifact | **NO** | Write `link-builder.error.json` on failure |
| Do not improvise | **NO** | Refuse to create links to non-existent nodes |

### deployer

| Criterion | Present? | Fix |
|-----------|----------|-----|
| Explicit I/O contracts | YES | — |
| Termination criteria | **NO** | Max 30 min |
| Bounded tool calls | **NO** | Allow: `fs-mcp.*`, `template-mcp.validate_html` |
| Deterministic mode | **NO** | Stable file copy order, deterministic minification |
| Failure artifact | **NO** | Write `deployer.error.json` on failure, do not update `docs/` |
| Do not improvise | **NO** | Refuse to modify source `sites/` |

---

## 4) MCP Boundary Review

### fs-mcp

| Tool | Risk | Issue | Fix |
|------|------|-------|-----|
| `write_file` | **HIGH** | No path validation | Allowlist: `sites/leo-global-holdings/**`, `docs/**`, `ventures.json` only |
| `delete_file` | **HIGH** | Could delete anything | Rename to `archive_file`, move to `.trash/` |
| `move_file` | **HIGH** | Path traversal possible | Validate both src and dst within allowed roots |
| `copy_file` | **HIGH** | Could copy secrets | Validate destination within allowed roots |

**Mandatory changes:**
- Path traversal protection (reject `..`, `~`, absolute paths outside root)
- File size limit: 10MB max
- All operations logged with timestamps

### content-mcp

| Tool | Risk | Issue | Fix |
|------|------|-------|-----|
| `expand_venture_list` | **MED** | LLM output unvalidated | Output schema validation required |
| All `generate_*` | **MED** | HTML injection risk | Sanitize output: strip `<script>`, event handlers, `javascript:` |

**Mandatory changes:**
- Output schema validation for all tools
- HTML sanitization before injection
- Max 4096 tokens per tool call
- Content policy filter

### template-mcp

| Tool | Risk | Issue | Fix |
|------|------|-------|-----|
| `render_template` | **LOW** | Template injection if user data in vars | Sanitize all variables before injection |
| `get_template` | **LOW** | Path traversal in template name | Allowlist of template names |

### graph-mcp

| Tool | Risk | Issue | Fix |
|------|------|-------|-----|
| `add_node` | **MED** | Could add arbitrary nodes | Validate path matches `leo-global-holdings/{group}/{venture}/{offering}` |
| `remove_node` | **MED** | Could remove critical nodes | Protect root and group nodes, require confirmation |

### registry-mcp

| Tool | Risk | Issue | Fix |
|------|------|-------|-----|
| `register_plugin` | **LOW** | Could register malicious plugins | Require approval before registration |
| `register_hook` | **LOW** | Could register dangerous hooks | Require approval, scan code |

---

## 5) Determinism/Idempotency Review

| Check | Present? | Fix |
|-------|----------|-----|
| runId + content hash | **NO** | Generate UUID `run_id` per run. Tag all outputs. SHA-256 hash of `ventures.json` for change detection. |
| Deterministic template versions | **NO** | Pin versions in `templates/VERSION`. Agents verify version match before render. |
| Stable sorting + canonicalization | **NO** | All JSON: sorted keys. All listings: alphabetical. All nav: deterministic order. |
| Concurrency lock | **NO** | Add `concurrency: { group: generate-${{ github.ref }}, cancel-in-progress: false }` to workflow. |
| Diff behavior (unchanged = no-op) | **NO** | Hash check in `parse-conversation.js`. Site-creator checks existing dirs/files. Content-enricher checks existing content. |

---

## 6) 24/7 Stop-the-Line Logic

| Check | Present? | Fix |
|-------|----------|-----|
| Health checks | **PARTIAL** | Add `/health` endpoints to MCP servers. Add `healthcheck` to `compose.yml`. |
| Alerting on failure | **NO** | Add email/Slack webhook notification on `failure()`. |
| Automatic retries with backoff | **NO** | Agent containers: `--restart on-failure:3`. MCP calls: retry wrapper with exponential backoff. |
| Circuit breaker | **NO** | Track failures in `runs/failure_count.json`. If 3 consecutive failures, halt publishing and alert. |
| Rate limits / throttles | **NO** | `content-mcp`: 60 calls/min limit. Agent max runtime: Docker `--stop-timeout`. |
| Resource/time budgets | **NO** | Each agent: `--memory 512m --cpus 1.0`. Total pipeline: 2 hours max. Exceed = kill all + alert. |

---

## 7) Minimal Patch List

### Critical (must fix before any run)

| # | Patch | Files |
|---|-------|-------|
| **C1** | Path allowlist in `fs-mcp`: reject writes outside `sites/`, `docs/`, `ventures.json` | `docker/mcp-servers/fs-mcp/server.py` |
| **C2** | Add `--security-opt no-new-privileges --user 1000:1000` to all agent containers | `docker/compose.yml`, agent Dockerfiles |
| **C3** | `OPENAI_API_KEY` via Docker secrets, not env vars | `docker/compose.yml`, `docker/mcp-servers/content-mcp/server.py` |
| **C4** | Add `concurrency` group to GitHub Actions workflow | `.github/workflows/generate-enterprise-sites.yml` |
| **C5** | Atomic `docs/` build: write to `.build/` then move | `docker/agents/deployer/agent.py` |
| **C6** | `validate_graph()` as hard gate before deploy | `.github/workflows/generate-enterprise-sites.yml` |

### High (must fix before production)

| # | Patch | Files |
|---|-------|-------|
| **H1** | Termination criteria (max iterations, max time) for all 5 agents | All agent `agent.py` |
| **H2** | Tool allowlists (explicit MCP tool names) for all 5 agents | All agent `agent.py` |
| **H3** | Failure artifact emission (`*.error.json`) for all 5 agents | All agent `agent.py` |
| **H4** | HTML sanitization on `content-mcp` output | `docker/mcp-servers/content-mcp/server.py` |
| **H5** | `healthcheck` for all MCP servers | `docker/compose.yml` |
| **H6** | Circuit breaker (3 failures = halt publishing) | `.github/workflows/generate-enterprise-sites.yml` |
| **H7** | Retry with backoff for MCP server calls | All agent `agent.py` |
| **H8** | Content hash diffing to skip unchanged ventures | `scripts/parse-conversation.js`, agents |

### Medium (fix before public launch)

| # | Patch | Files |
|---|-------|-------|
| **M1** | Broken link check as hard gate | `docker/agents/deployer/agent.py` |
| **M2** | Rate limiting on `content-mcp` | `docker/mcp-servers/content-mcp/server.py` |
| **M3** | Resource limits (`--memory`, `--cpus`) on all containers | `docker/compose.yml` |
| **M4** | Deterministic JSON serialization (sorted keys) | All agent `agent.py` |
| **M5** | Template version pinning | `templates/VERSION`, all agents |
| **M6** | Failure notification (email/Slack) | `.github/workflows/generate-enterprise-sites.yml` |
| **M7** | Run ID tagging on all output files | All agents |
| **M8** | "Do not improvise" guard (refuse out-of-scope) | All agent prompt files |

---

## Updated Agent Contract Template

Every agent must include this contract header:

```yaml
agent: venture-generator
version: 1.0.0
run_id: "${RUN_ID}"
timeout_seconds: 1800
max_iterations: 500

input:
  - path: ventures.json
    schema: ventures-v2
  - path: memory/chatgpt-conversation-andrew-franklin-leo.md
    type: markdown

output:
  - path: ventures.json
    schema: ventures-v2
  - path: "*.error.json"
    condition: on_failure

allowed_tools:
  - content-mcp.expand_venture_list
  - content-mcp.generate_venture_description
  - graph-mcp.add_node
  - graph-mcp.find_gaps
  - fs-mcp.read_json
  - fs-mcp.write_json

denied_tools:
  - fs-mcp.delete_file
  - fs-mcp.move_file
  - "*exec*"
  - "*eval*"
  - "*subprocess*"

paths:
  read:
    - ventures.json
    - memory/chatgpt-conversation-andrew-franklin-leo.md
  write:
    - ventures.json
    - sites/leo-global-holdings/**/_venture.json

on_failure:
  write: ventures.json.error
  exit_code: 1
  alert: true
```

---

## Updated MCP Server Contract Template

```yaml
server: fs-mcp
version: 1.0.0

tools:
  - name: read_file
    params: { path: string }
    returns: string
    path_constraint: "INHERITED_FROM_ROOT"

  - name: write_file
    params: { path: string, content: string }
    returns: boolean
    path_constraint: "sites/leo-global-holdings/** OR docs/** OR ventures.json"
    max_size: 10MB

  - name: delete_file
    params: { path: string }
    returns: boolean
    path_constraint: "BLOCKED"  # Renamed to archive_file
    replacement: archive_file

security:
  no_shell: true
  no_exec: true
  no_eval: true
  log_all_operations: true
  max_file_size: 10MB
```

---

## Updated compose.yml Security Additions

```yaml
services:
  fs-mcp:
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      - ./sites:/data/sites:rw
      - ./docs:/data/docs:rw
      - ./ventures.json:/data/ventures.json:rw
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:3001/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  agent-template:
    security_opt:
      - no-new-privileges:true
    user: "1000:1000"
    mem_limit: 512m
    cpus: 1.0
    stop_grace_period: 30s
```

---

## Updated GitHub Actions Guardrails

```yaml
concurrency:
  group: generate-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    # Runs BEFORE deploy
    steps:
      - name: Validate graph integrity
        run: docker run --rm --network host agents/validator --check graph
      - name: Check broken links
        run: docker run --rm --network host agents/validator --check links
      - name: Diff validation
        run: node scripts/validate-diff.js

  deploy:
    needs: [validate]  # Only deploys if validation passes
    steps:
      - name: Atomic docs build
        run: |
          mkdir -p .build
          # ... build to .build ...
          # ... validate .build ...
          Move-Item -Force .build docs
```
